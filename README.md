# DevTinder

-Created a Vite + React application
-Remove unecessary code and create a hello world app
- Tailwind css installed 
- Install Daisy UI
- Add Navbar componenet to App.jsx 
-create a NavBar.jsx seperate Components file
-install react router dom
-Create BrowserRouter > Routes > Route=/ Body > RouteChildren
-Create an Outlet in Body Component
-Create a footer
-Create the Login Page 
-install axios
-CORS -install cors in backend => add the middleware to with configuration: origin, credential: true
-Whenever making API call so pass axios =>{withCredentials: true}
-install react-redux + @reduxjs/toolkit => configureStore => Provider => createSlice => add reducer to store
-Add redux devtools in chrome
-Login and seen if data is comming properly in the store
-NavBar should update as soon as user logs in
-Refactor our code to add constants file + create a componenets folder


Body
   NavBar
   Route=/ => Feed
   Route=/login =>Login
   Route=/connections =>connection
   Route=/profile =>Profile